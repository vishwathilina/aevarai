spring:
  application:
    name: auction
  datasource:

    url: jdbc:postgresql://localhost:5432/auction_db_4
    username: postgres
    password: 2004
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# --- STRIPE CONFIGURATION ---
# Set these environment variables before running the application:
# STRIPE_API_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PUBLISHABLE_KEY
stripe:
  api-key: ${STRIPE_API_KEY:your_stripe_secret_key_here}
  webhook-secret: ${STRIPE_WEBHOOK_SECRET:your_webhook_secret_here}
  publishable-key: ${STRIPE_PUBLISHABLE_KEY:your_publishable_key_here}